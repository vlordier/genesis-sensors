# Release Guide

This page summarizes the release flow for `genesis-sensors`.

## Summary

1. Merge work into `develop`
2. Open release PR from `develop` to `main`
3. Bump version and update changelog
4. Tag `vX.Y.Z`
5. GitHub Actions publishes to PyPI
